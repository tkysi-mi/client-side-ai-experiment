{
    admin off
    persist_config off
    auto_https off
}

:{$PORT:3000} {
    @health path /health
    respond @health 200

    root * /app/dist
    encode gzip
    try_files {path} /index.html
    file_server
}
